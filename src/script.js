// Create the "Ajouter un livre" button

const addBookButton = document.getElementById('btn-add');;
addBookButton.id = "addBook";
addBookButton.innerHTML = "Ajouter un livre";
divMyBooks.appendChild(addBookButton);

});